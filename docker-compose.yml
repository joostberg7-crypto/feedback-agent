services:
  backend:
    image: suham1/feedback-backend:latest
    ports:
      - "9050:8080"
    env_file:
      - ./backend/.env
    restart: always

  frontend:
    image: suham1/feedback-frontend:latest
    ports:
      - "9051:80"
    restart: always